/*export const useUserListStore = defineStore('userList', () => {
    const name = ref('WSWWT')
    const showList = ref([])

    function addToList(show) {
        showList.push(show)
    }

    function deleteFromList(show) {
        showList.push(show)
    }

    return { name, showList, addToList }
})*/